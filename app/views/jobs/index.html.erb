All Jobs for <strong><%= @company.name %></strong>
<p><%= link_to "<< Back to Companies", companies_path %></p>

<% @jobs.each do |job|%>
  <ul>
      <%= job.title %> at <%= job.company.name %> :
      <%= link_to "View", company_job_path(job.company, job) %>
      <%= link_to "Edit", edit_company_job_path(job.company, job) %>
      <%= link_to "Delete", company_job_path(job.company, job), method: 'delete' %>
  </ul>
<% end %>

<%= render partial: 'contacts/contact' %>
<h1>Enter your contact information</h1>
<%= render partial: 'contacts/form'  %>
<%= link_to "Create a New Job", new_company_job_path(@company.id) %>
